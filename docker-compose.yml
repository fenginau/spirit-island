services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
            args:
                GEMINI_API_KEY: ${GEMINI_API_KEY:-}
        image: spirit-island-spirit-picker:prod
        ports:
            - '28080:80'
        restart: unless-stopped
